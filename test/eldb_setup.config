{mode, max}.

{duration, infinity}.

{concurrent, 1}.

{code_paths, ["ebin"]}.

{source_dir, "test"}.

{driver, basho_bench_driver_eldb}.

{key_generator, {sequential_int, 10000}}.

{value_generator, {fixed_bin, 10000}}.

{operations, [{batch_put, 1}]}.

